[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "xrf-watermark-lab"
version = "0.1.0"
description = "Research harness for session-attributable agent text watermarks"
requires-python = ">=3.10"
dependencies = []

[project.optional-dependencies]
openai = ["openai>=1.0"]

[tool.setuptools.packages.find]
include = ["watermark_lab*"]
